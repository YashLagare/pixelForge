import { StudioWorkbenchProps } from "@/lib/types"

const StudioWorkbench = ({
    clerkUserId,
    initialHistory,
    initialQuota,
}: StudioWorkbenchProps) => {
    return (
        <div>StudioWorkbench</div>
    )
}

export default StudioWorkbench